# Copy to server.test.env (gitignored) and adjust credentials.
# Point DATABASE_URL at a dedicated Postgres database used only for Vitest.
# Run migrations against that DB once: DATABASE_URL=... pnpm migrate

DATABASE_URL=postgresql://username:password@localhost:5432/meal_prep_app_db_tests
